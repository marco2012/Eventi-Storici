var date = require("../models/date")

exports.retreive = function(date, callback) {
        //Logica da inserire per reperire l'oggetto o da database o dal muffinlabs
        //Penso che qua si debba anche inserire la logica per far partire il prcesso con rabbit,
        //ma lo vedremo.
}
